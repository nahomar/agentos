[package]
name = "agentos-kernel"
version = "0.1.0"
edition = "2021"
description = "AgentOS — Microkernel for Agentic Computing"
license = "MIT"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tokio-test = "0.4"
